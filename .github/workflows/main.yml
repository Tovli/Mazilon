name: "Build production artifacts"
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && 'main-release-pipeline' || github.run_id }}
  cancel-in-progress: false
  queue: max

jobs:
  functions-test:
    name: Cloud Functions tests and production audit
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: functions
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: functions/package-lock.json

      - name: Install Functions dependencies
        run: npm ci

      - name: Run Functions tests
        run: npm test

      - name: Test Functions deployment change detection
        run: node --test ../scripts/tests/functions_deployment_changes.test.mjs

      - name: Audit production dependencies
        # Production deployment must remain free of known audit findings.
        run: npm audit --omit=dev --audit-level=low

  build-android:
    runs-on: ubuntu-latest
    env:
      KEYSTORE_PATH: android/app/
      KEYSTORE_FILE: Maztzilonkey.jks
      FIREBASE_OPTIONS_PATH: lib/util/Firebase/firebase_options.dart
      AAB_PATH: build/app/outputs/bundle/release/app-release.aab
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Write key store file for release signing
        env:
          APPSIGNINGKEY: ${{ secrets.APPSIGNINGKEY }}
        run: |
          printf '%s' "$APPSIGNINGKEY" | base64 --decode > "${{ env.KEYSTORE_PATH }}${{ env.KEYSTORE_FILE }}"

      - name: Write firebase options file
        env:
          FIREBASE_OPTIONS: ${{ secrets.FIREBASE_OPTIONS }}
        run: |
          printf '%s' "$FIREBASE_OPTIONS" | base64 --decode > "${{ env.FIREBASE_OPTIONS_PATH }}"

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: "adopt"
          java-version: "17.x"
          cache: gradle
          cache-dependency-path: |
            .github/workflows/main.yml
            android/**/*.gradle*
            android/gradle.properties
            android/gradle/wrapper/gradle-wrapper.properties
            pubspec.yaml
            pubspec.lock

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.0"
          cache: true
      - name: Install dependencies
        shell: bash
        run: |
          export XDG_CONFIG_HOME="$RUNNER_TEMP/dart-config"
          mkdir -p "$XDG_CONFIG_HOME"
          echo "XDG_CONFIG_HOME=$XDG_CONFIG_HOME" >> "$GITHUB_ENV"
          for attempt in 1 2 3; do
            echo "Installing Dart dependencies (attempt ${attempt}/3)."
            if flutter pub get; then
              exit 0
            fi
            if [[ "$attempt" -lt 3 ]]; then
              delay=$((attempt * 15))
              echo "::warning::flutter pub get failed; retrying in ${delay}s."
              sleep "$delay"
            fi
          done
          exit 1
      - name: Run tests with coverage
        run: flutter test --coverage --reporter=expanded
      - name: Test iOS VM-service recovery guards
        run: |
          bash scripts/tests/recover_ios_vm_service_test.sh
          python3 scripts/tests/recover_ios_vm_service_cli_test.py
      - name: Snapshot base lcov before dart-define run
        # The next `flutter test` invocation overwrites coverage/lcov.info;
        # snapshot the full-suite lcov so we can merge the dart-define
        # variant on top of it via scripts/merge_lcov.dart. See
        # docs/adr/ADR-001-phase-6-test-coverage-integration-tests.md.
        run: cp coverage/lcov.info coverage/lcov.base.info
      - name: Run AnalyticsService tests with MIXPANEL_PROJECT_TOKEN dart-define
        # Phase 6 (ADR-001): MixPanelService.init / trackEvent post-init
        # branches are gated on a non-empty
        # String.fromEnvironment('MIXPANEL_PROJECT_TOKEN'). This second test
        # invocation exercises only the AnalyticsService token-present
        # branches with the env var set; the standard suite already covered
        # the empty-token short-circuit branches in the run above.
        run: |
          flutter test --coverage \
            --dart-define=MIXPANEL_PROJECT_TOKEN=test-token \
            test/AnalyticsService/MixPanelService_token_test.dart
          cp coverage/lcov.info coverage/lcov.token.info
      - name: Merge lcov artifacts
        run: dart run scripts/merge_lcov.dart coverage/lcov.info coverage/lcov.base.info coverage/lcov.token.info
      - name: Enforce coverage gate
        run: dart run scripts/check_coverage.dart
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-lcov
          path: coverage/lcov.info

      - name: Get version from pubspec.yaml
        id: get_version
        run: |
          VERSION_LINE=$(grep '^version: ' pubspec.yaml)
          VERSION_STRING=${VERSION_LINE#version: }
          echo "VERSION_STRING=$VERSION_STRING" >> $GITHUB_ENV

      - name: Parse version components
        run: |
          VERSION_NO_BUILD="${VERSION_STRING%%+*}"
          echo "VERSION_NO_BUILD=$VERSION_NO_BUILD" >> $GITHUB_ENV
      - name: Get short commit hash
        run: |
          COMMIT_HASH=$(git rev-parse --short HEAD)
          echo "COMMIT_HASH=$COMMIT_HASH" >> $GITHUB_ENV

      - name: Set build name and number
        run: |
          BUILD_NAME="${VERSION_NO_BUILD}+${COMMIT_HASH}"
          BUILD_NUMBER="${GITHUB_RUN_NUMBER}"
          echo "BUILD_NAME=$BUILD_NAME" >> $GITHUB_ENV
          echo "BUILD_NUMBER=$BUILD_NUMBER" >> $GITHUB_ENV

      - name: Build App Bundle
        env:
          APPSIGNINGKEYPASSWORD: ${{ secrets.APPSIGNINGKEYPASSWORD }}
          APPSIGNINGKEYSTOREPASSWORD: ${{ secrets.APPSIGNINGKEYSTOREPASSWORD }}
          MIXPANEL_PROJECT_TOKEN: ${{ secrets.MIXPANEL_PROJECT_TOKEN }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          GOOGLE_SIGN_IN_SERVER_CLIENT_ID: ${{ secrets.GOOGLE_SIGN_IN_SERVER_CLIENT_ID }}
        run: |
          if [ "$GITHUB_REF" = "refs/heads/main" ] && [ -z "$(printf '%s' "$GOOGLE_SIGN_IN_SERVER_CLIENT_ID" | tr -d '[:space:]')" ]; then
            echo "::error title=Missing Google Sign-In configuration::Set the GOOGLE_SIGN_IN_SERVER_CLIENT_ID repository secret before releasing from main."
            exit 1
          fi
          flutter build appbundle --release \
            --build-name="${BUILD_NAME}" \
            --build-number="${BUILD_NUMBER}" \
            --dart-define=MIXPANEL_PROJECT_TOKEN="$MIXPANEL_PROJECT_TOKEN" \
            --dart-define=SENTRY_DSN="$SENTRY_DSN" \
            --dart-define=GOOGLE_SIGN_IN_SERVER_CLIENT_ID="$GOOGLE_SIGN_IN_SERVER_CLIENT_ID"

      - name: Upload App Bundle
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: ${{ env.AAB_PATH }}

      # - name: Build APK
      #   run: flutter build apk --release
      # - name: Upload APK
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: app-release-apk
      #     path: ${{ env.APK_PATH }}

  integration-test-shard:
    name: integration-test-shard (${{ matrix.id }})
    # ADR-010: keep each Android integration entry point in its own runner so
    # repeated native builds cannot exhaust one runner's disk. The downstream
    # integration-test job preserves the required-check and artifact contracts.
    needs: build-android
    runs-on: ubuntu-latest
    # Bound emulator hangs without changing the coverage or isolation contract.
    timeout-minutes: 45
    concurrency:
      group: ${{ format('{0}-{1}-{2}', github.workflow, github.ref, matrix.id) }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        include:
          - id: custom-categories
            test_file: integration_test/custom_categories_e2e_test.dart
          - id: dark-mode-settings
            test_file: integration_test/dark_mode_settings_test.dart
          - id: bootstrap-smoke
            test_file: integration_test/bootstrap_smoke_test.dart
          - id: bootstrap-full
            test_file: integration_test/bootstrap_full_test.dart
          - id: wellness-player
            test_file: integration_test/wellness_player_test.dart
          - id: logger-init
            test_file: integration_test/logger_init_test.dart
          - id: notifications-schedule
            test_file: integration_test/notifications_schedule_test.dart
          - id: personal-plan-info-modal
            test_file: integration_test/personal_plan_info_modal_test.dart
    env:
      FIREBASE_OPTIONS_PATH: lib/util/Firebase/firebase_options.dart
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Write firebase options file
        env:
          FIREBASE_OPTIONS: ${{ secrets.FIREBASE_OPTIONS }}
        run: |
          printf '%s' "$FIREBASE_OPTIONS" | base64 --decode > "${{ env.FIREBASE_OPTIONS_PATH }}"

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: "adopt"
          java-version: "17.x"
          cache: gradle
          cache-dependency-path: |
            .github/workflows/main.yml
            android/**/*.gradle*
            android/gradle.properties
            android/gradle/wrapper/gradle-wrapper.properties
            pubspec.yaml
            pubspec.lock

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.0"
          cache: true

      - name: Install dependencies
        shell: bash
        run: |
          export XDG_CONFIG_HOME="$RUNNER_TEMP/dart-config"
          mkdir -p "$XDG_CONFIG_HOME"
          echo "XDG_CONFIG_HOME=$XDG_CONFIG_HOME" >> "$GITHUB_ENV"
          for attempt in 1 2 3; do
            echo "Installing Dart dependencies (attempt ${attempt}/3)."
            if flutter pub get; then
              exit 0
            fi
            if [[ "$attempt" -lt 3 ]]; then
              delay=$((attempt * 15))
              echo "::warning::flutter pub get failed; retrying in ${delay}s."
              sleep "$delay"
            fi
          done
          exit 1

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666"' | \
            sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules && sudo udevadm trigger --name-match=kvm

      - name: Run integration test shard on Android emulator
        uses: reactivecircus/android-emulator-runner@a421e43855164a8197daf9d8d40fe71c6996bb0d # v2.38.0
        with:
          api-level: 34
          target: google_apis
          arch: x86_64
          profile: pixel_6
          # The synthetic, non-routable SENTRY_DSN exercises logger
          # initialization; native Sentry channels are mocked, so no network
          # traffic leaves the runner. The explicit device keeps the test on
          # the emulator shown by `flutter devices`. The notifications schedule
          # shard is an FCM policy smoke test, not a retired local-scheduler test.
          # `>-` is load-bearing: android-emulator-runner executes each non-empty
          # line in a separate `sh -c`; the test exit code must stay in one
          # folded command through the post-test disk diagnostic and final exit.
          script: >-
            mkdir -p coverage/shards;
            flutter devices;
            echo "Filesystem capacity before integration test shard:";
            df -h;
            flutter test "${{ matrix.test_file }}" --coverage --coverage-path "coverage/shards/${{ matrix.id }}.info" --dart-define=SENTRY_DSN=https://test@dsn.example.local/0 -d emulator-5554;
            test_exit_code=$?;
            echo "Filesystem capacity after integration test shard:";
            df -h;
            exit "$test_exit_code"

      - name: Assert post-test disk headroom
        if: always()
        shell: bash
        run: |
          available_kb=$(df -Pk . | awk 'NR == 2 { print $4 }')
          if [[ ! "$available_kb" =~ ^[0-9]+$ ]]; then
            echo "::error::Could not parse available disk space from df -Pk." >&2
            exit 1
          fi
          available_mib=$((available_kb / 1024))
          echo "Post-test disk available: ${available_mib} MiB (${available_kb} KB)"
          if (( available_kb < 2097152 )); then
            echo "::warning::Post-test disk headroom is below 2 GiB (${available_mib} MiB available)."
          fi

      - name: Upload integration coverage shard
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-integration-shard-${{ matrix.id }}
          path: coverage/shards/${{ matrix.id }}.info
          if-no-files-found: warn

  integration-test-inventory:
    # Run test discovery beside the emulator shards so matrix drift fails
    # without extending the canonical coverage gate's critical path.
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify Android integration matrix matches test files
        shell: bash
        run: |
          mapfile -t declared < <(
            awk '
              /^  integration-test-shard:$/ { in_shard = 1; next }
              in_shard && /^  [[:alnum:]_-]+:$/ { exit }
              in_shard && /^[[:space:]]+test_file:[[:space:]]*/ {
                sub(/^[[:space:]]*test_file:[[:space:]]*/, "")
                print
              }
            ' .github/workflows/main.yml | sort
          )
          mapfile -t discovered < <(
            find integration_test -type f -name '*_test.dart' ! -name '*_ios_test.dart' -printf '%p\n' | sort
          )

          printf 'Declared Android integration matrix files:\n'
          printf '  %s\n' "${declared[@]}"
          printf 'Discovered non-iOS integration test files:\n'
          printf '  %s\n' "${discovered[@]}"

          if [[ "${#declared[@]}" -ne "${#discovered[@]}" ]] || \
             [[ "$(printf '%s\n' "${declared[@]}")" != "$(printf '%s\n' "${discovered[@]}")" ]]; then
            echo "::error::Declared Android integration matrix files do not match discovered non-iOS integration tests." >&2
            exit 1
          fi

  integration-test:
    # Phase 7 (ADR-002) required-check contract, implemented as the canonical
    # ADR-010 merge and per-file coverage gate for the Android shard matrix.
    needs: [integration-test-shard, integration-test-inventory]
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Require successful integration-test prerequisites
        shell: bash
        run: |
          shard_result="${{ needs.integration-test-shard.result }}"
          inventory_result="${{ needs.integration-test-inventory.result }}"
          echo "integration-test-shard result: $shard_result"
          echo "integration-test-inventory result: $inventory_result"
          prerequisite_failed=0
          if [[ "$shard_result" != "success" ]]; then
            echo "::error::Upstream integration-test-shard matrix did not succeed (result=$shard_result)." >&2
            prerequisite_failed=1
          fi
          if [[ "$inventory_result" != "success" ]]; then
            echo "::error::Upstream integration-test-inventory did not succeed (result=$inventory_result)." >&2
            prerequisite_failed=1
          fi
          if [[ "$prerequisite_failed" -ne 0 ]]; then
            exit 1
          fi

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.0"
          cache: true

      - name: Install dependencies
        shell: bash
        run: |
          export XDG_CONFIG_HOME="$RUNNER_TEMP/dart-config"
          mkdir -p "$XDG_CONFIG_HOME"
          echo "XDG_CONFIG_HOME=$XDG_CONFIG_HOME" >> "$GITHUB_ENV"
          for attempt in 1 2 3; do
            echo "Installing Dart dependencies (attempt ${attempt}/3)."
            if flutter pub get; then
              exit 0
            fi
            if [[ "$attempt" -lt 3 ]]; then
              delay=$((attempt * 15))
              echo "::warning::flutter pub get failed; retrying in ${delay}s."
              sleep "$delay"
            fi
          done
          exit 1

      - name: Download integration coverage shards
        uses: actions/download-artifact@v4
        with:
          pattern: coverage-integration-shard-*
          merge-multiple: true
          path: coverage/shards

      - name: Verify integration coverage shard inventory
        shell: bash
        run: |
          mkdir -p coverage/shards
          mapfile -t expected_sorted < <(
            awk '
              /^  integration-test-shard:$/ { in_shard = 1; next }
              in_shard && /^  [[:alnum:]_-]+:$/ { exit }
              in_shard && /^[[:space:]]+-[[:space:]]+id:[[:space:]]*/ {
                sub(/^[[:space:]]+-[[:space:]]+id:[[:space:]]*/, "")
                print $0 ".info"
              }
            ' .github/workflows/main.yml | sort
          )
          mapfile -t actual_sorted < <(find coverage/shards -maxdepth 1 -type f -printf '%f\n' | sort)

          printf 'Expected coverage shards:\n'
          printf '  %s\n' "${expected_sorted[@]}"
          printf 'Downloaded coverage shards:\n'
          printf '  %s\n' "${actual_sorted[@]}"

          if [[ "${#actual_sorted[@]}" -ne "${#expected_sorted[@]}" ]] || \
             [[ "$(printf '%s\n' "${actual_sorted[@]}")" != "$(printf '%s\n' "${expected_sorted[@]}")" ]]; then
            echo "::error::Downloaded integration coverage shard set does not match the declared expected inventory." >&2
            exit 1
          fi

      - name: Merge integration coverage shards
        run: dart run scripts/merge_lcov.dart coverage/integration.info coverage/shards/*.info

      - name: Enforce integration-test per-file coverage floors
        run: dart run scripts/check_integration_coverage.dart

      - name: Upload integration coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-integration-lcov
          path: coverage/integration.info

  integration-test-ios:
    # Blocking iOS simulator validation. This job runs the iOS-specific
    # FCM initialization test and fails the workflow when
    # the test is missing or fails. It intentionally does not collect lcov;
    # aggregate coverage remains the unit + Android integration union.
    runs-on: macos-15
    # Simulator validation must be time-bounded. GitHub's default timeout is
    # 360 minutes, which lets a stuck `flutter test` monopolize the macOS
    # runner long after the blocking gates have completed.
    # A cold CocoaPods/Firebase compile can exceed 45 minutes on GitHub's
    # arm64 macOS runners. Allow the first run to finish and populate the
    # DerivedData cache while keeping the job bounded. Leave enough time after
    # the 90-minute test step to save a cold build and upload diagnostics.
    # The recorded cold setup is about 42 minutes, so 150 minutes preserves
    # 18 minutes for post-test forensics and cache persistence.
    timeout-minutes: 150
    env:
      FIREBASE_OPTIONS_PATH: lib/util/Firebase/firebase_options.dart
      IOS_PODS_CACHE_VERSION: v1
      IOS_DERIVED_DATA_CACHE_VERSION: v1
      # cloud_firestore 6.4.1's SwiftPM manifest allows firebase-ios-sdk
      # 12.14.0, whose Firestore pipeline bridge changed initializer
      # signatures. Keep this iOS simulator job on the CocoaPods path until
      # FlutterFire updates cloud_firestore's iOS parser for that API.
      FLUTTER_SWIFT_PACKAGE_MANAGER: "false"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Write firebase options file
        env:
          FIREBASE_OPTIONS: ${{ secrets.FIREBASE_OPTIONS }}
        run: |
          printf '%s' "$FIREBASE_OPTIONS" | base64 --decode > "${{ env.FIREBASE_OPTIONS_PATH }}"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.0"
          cache: true

      - name: Install dependencies
        shell: bash
        run: |
          export XDG_CONFIG_HOME="$RUNNER_TEMP/dart-config"
          mkdir -p "$XDG_CONFIG_HOME"
          echo "XDG_CONFIG_HOME=$XDG_CONFIG_HOME" >> "$GITHUB_ENV"
          for attempt in 1 2 3; do
            echo "Installing Dart dependencies (attempt ${attempt}/3)."
            if flutter pub get; then
              exit 0
            fi
            if [[ "$attempt" -lt 3 ]]; then
              delay=$((attempt * 15))
              echo "::warning::flutter pub get failed; retrying in ${delay}s."
              sleep "$delay"
            fi
          done
          exit 1

      - name: Capture Xcode version for cache keys
        id: xcode
        run: |
          xcodebuild -version
          XCODE_CACHE_KEY=$(xcodebuild -version | tr '\n' '-' | sed -E 's/-$//' | tr ' ' '_')
          echo "cache_key=$XCODE_CACHE_KEY" >> "$GITHUB_OUTPUT"

      - name: Cache CocoaPods
        uses: actions/cache@v5
        with:
          path: |
            ios/Pods
            ~/Library/Caches/CocoaPods
            ~/.cocoapods/repos
          key: ${{ runner.os }}-${{ runner.arch }}-${{ steps.xcode.outputs.cache_key }}-ios-pods-${{ env.IOS_PODS_CACHE_VERSION }}-${{ hashFiles('pubspec.lock', 'ios/Podfile', 'ios/Podfile.lock', '.flutter-plugins-dependencies') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-${{ steps.xcode.outputs.cache_key }}-ios-pods-${{ env.IOS_PODS_CACHE_VERSION }}-

      - name: Cache Xcode DerivedData
        id: ios-derived-data-cache
        uses: actions/cache/restore@v5
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: ${{ runner.os }}-${{ runner.arch }}-${{ steps.xcode.outputs.cache_key }}-ios-deriveddata-${{ env.IOS_DERIVED_DATA_CACHE_VERSION }}-${{ hashFiles('pubspec.lock', 'ios/Podfile', 'ios/Podfile.lock', '.flutter-plugins-dependencies', 'ios/Runner.xcodeproj/project.pbxproj', 'ios/Runner.xcworkspace/contents.xcworkspacedata') }}
          # DerivedData contains build products tied to the exact plugin/pod
          # graph. Prefix fallback restored a removed workmanager framework,
          # producing a stale Runner binary that exited in dyld on launch.
          # Use exact matches only; CocoaPods keeps its own broader cache.

      - name: Resolve CocoaPods dependencies with current specs
        working-directory: ios
        # --deployment rejects any Podfile/Podfile.lock mutation. Updating the
        # specs index may locate locked podspecs, but cannot change versions.
        run: pod install --repo-update --deployment

      - name: List available iOS simulators
        # Diagnostic: surfaces the runtime + device names available on the
        # macos-15 image. If the next step fails to find an iPhone, the
        # output here is the first place to look.
        run: xcrun simctl list devices available

      - name: Boot iPhone simulator
        # The iOS FCM test does not depend on a particular simulator runtime.
        # Select the first available iPhone so routine macOS image rotations do
        # not block the release pipeline before the test can run.
        timeout-minutes: 10
        run: |
          DEVICE_LINE=$(xcrun simctl list devices available | awk '/^[[:space:]]+iPhone / { print; exit }')
          DEVICE_ID=$(echo "$DEVICE_LINE" | awk -F '[()]' '{print $2}')
          DEVICE_NAME=$(echo "$DEVICE_LINE" | sed -E 's/^\s+//; s/ \(.*//')
          if [ -z "$DEVICE_ID" ]; then
            echo "::error::No available iPhone simulator found on this macos-15 runner."
            xcrun simctl list devices available
            exit 1
          fi
          echo "Using simulator: $DEVICE_NAME ($DEVICE_ID)"
          echo "DEVICE_ID=$DEVICE_ID" >> $GITHUB_ENV
          xcrun simctl boot "$DEVICE_ID"
          # bootstatus blocks until the sim is fully booted (springboard up).
          xcrun simctl bootstatus "$DEVICE_ID" -b

      - name: Run iOS integration test with diagnostics
        id: ios-integration-test
        # Notes on the invocation below (mirrors the Android job's design
        # rationale in the integration-test job above):
        #
        # 1. Single-line `flutter test` — no `\<newline>` continuations.
        # 2. `-d "$DEVICE_ID"` explicitly targets the booted simulator. Without
        #    `-d`, Flutter sometimes falls back to host-VM mode which
        #    re-triggers "Integration tests and unit tests cannot be run in a
        #    single invocation."
        # 3. SENTRY_DSN dart-define is a synthetic test value — Sentry's
        #    native SDK never reaches the network because the channel is
        #    mocked inside the test setUp. Symmetric with the Android job.
        # 4. Run only the iOS FCM initialization test here rather than
        #    duplicating the Android integration matrix on the iOS simulator.
        # A cold Xcode build plus VM-service discovery reached 73m49s on the
        # dependency-locked graph, then passed. Keep bounded headroom for that
        # proven path instead of timing out a successful native test.
        timeout-minutes: 90
        run: |
          mkdir -p ci-ios-diagnostics
          source scripts/recover_ios_vm_service.sh
          {
            echo "=== date ==="
            date -u
            echo
            echo "=== selected simulator ==="
            echo "$DEVICE_ID"
            xcrun simctl list devices "$DEVICE_ID"
            echo
            echo "=== flutter devices -v ==="
            flutter devices -v
          } > ci-ios-diagnostics/preflight.txt 2>&1 || true

          hold_diagnostic_owner() {
            # BASHPID is unavailable in macOS Bash 3.2. This direct child
            # stops its parent (the group leader), not the top-level shell.
            /bin/sh -c 'kill -STOP "$PPID"'
          }
          terminate_process_group() {
            local owner_pid="$1" identity="$2" current_identity
            local recorded_pid group_id parent_pid started live_members attempt state
            [[ "$owner_pid" =~ ^[1-9][0-9]*$ ]] || return 1
            [[ " ${RETIRED_DIAGNOSTIC_OWNERS:-} " != *" $owner_pid "* ]] || return 1
            read -r recorded_pid group_id parent_pid started <<< "$identity"
            if [ "$recorded_pid" != "$owner_pid" ] ||
               [[ ! "$group_id" =~ ^[1-9][0-9]*$ ]] ||
               [ "$group_id" != "$owner_pid" ] || [ "$parent_pid" != "$$" ]; then
              echo '::error::Diagnostic group ownership is unverified; refusing to signal it.'
              return 1
            fi
            current_identity=$(ps -o pid=,pgid=,ppid=,lstart= -p "$owner_pid") || return 1
            [ "$current_identity" = "$identity" ] || return 1
            # Freeze the group, then verify its anchored leader is still ours.
            # ps/kill are not atomic against an external SIGKILL; fail closed
            # on observed identity changes and never re-signal a retired ID.
            kill -STOP -- "-$group_id" 2>/dev/null || return 1
            for ((attempt = 0; attempt < 20; attempt++)); do
              current_identity=$(ps -o pid=,pgid=,ppid=,lstart= -p "$owner_pid") || return 1
              [ "$current_identity" = "$identity" ] || return 1
              state=$(ps -o stat= -p "$owner_pid") || return 1
              [[ "$state" != *T* ]] || break
              sleep 0.01
            done
            [[ "$state" == *T* ]] || return 1
            kill -KILL -- "-$group_id" 2>/dev/null || return 1
            # Even if the following ps fails, EXIT cleanup must not signal
            # this numeric identity a second time after it has been retired.
            RETIRED_DIAGNOSTIC_OWNERS="${RETIRED_DIAGNOSTIC_OWNERS:-} $owner_pid"
            for ((attempt = 0; attempt < 20; attempt++)); do
              live_members=$(ps -axo pgid=,stat= | awk -v group="$group_id" '$1 == group && $2 !~ /^Z/ { print }') || return 1
              if [ -z "$live_members" ]; then
                wait "$owner_pid" 2>/dev/null || true
                return 0
              fi
              sleep 0.1
            done
            echo '::error::Diagnostic process group did not stop; refusing to continue the test attempt.'
            return 1
          }

          stop_vm_service_recovery() {
            if [ -n "${VM_RECOVERY_PID:-}" ]; then
              terminate_process_group "$VM_RECOVERY_PID" "${VM_RECOVERY_IDENTITY:-}" || return 1
              VM_RECOVERY_PID=''
              VM_RECOVERY_IDENTITY=''
            fi
          }

          cleanup_ios_diagnostics() {
            local original_exit=$? cleanup_failed=0
            trap - EXIT
            stop_vm_service_recovery || cleanup_failed=1
            if [ -n "${SIM_LOG_PID:-}" ]; then
              terminate_process_group "$SIM_LOG_PID" "${SIM_LOG_IDENTITY:-}" || cleanup_failed=1
            fi
            if [ -n "${WATCHDOG_PID:-}" ]; then
              terminate_process_group "$WATCHDOG_PID" "${WATCHDOG_IDENTITY:-}" || cleanup_failed=1
            fi
            if [ "$cleanup_failed" -ne 0 ]; then
              echo '::warning::One or more diagnostic jobs could not be cleaned up.'
            fi
            exit "$original_exit"
          }
          trap cleanup_ios_diagnostics EXIT

          # Keep each group leader until cleanup, even when its command exits.
          # This prevents ordinary child completion from freeing its group ID.
          # The foreground Flutter job is always in a separate group.
          set -m
          (
            set +m
            trap hold_diagnostic_owner EXIT
            xcrun simctl spawn "$DEVICE_ID" log stream \
              --style compact \
              --level debug \
              --predicate 'process == "Runner" OR process == "SpringBoard" OR eventMessage CONTAINS "Flutter" OR eventMessage CONTAINS "Observatory" OR eventMessage CONTAINS "VMService"' \
              > ci-ios-diagnostics/simulator.log 2>&1
          ) &
          SIM_LOG_PID=$!
          SIM_LOG_IDENTITY=$(ps -o pid=,pgid=,ppid=,lstart= -p "$SIM_LOG_PID")

          (
            set +m
            trap hold_diagnostic_owner EXIT
            sleep 4800
            {
              echo "=== iOS test watchdog snapshot at 80 minutes ==="
              date -u
              echo
              echo "=== process snapshot ==="
              ps -axo pid,ppid,etime,command | grep -E 'flutter|dart|xcodebuild|simctl|Runner|ios-deploy' | grep -v grep || true
              echo
              echo "=== simulator state ==="
              xcrun simctl list devices "$DEVICE_ID"
            } > ci-ios-diagnostics/watchdog.txt 2>&1
            xcrun simctl io "$DEVICE_ID" screenshot ci-ios-diagnostics/watchdog-screenshot.png > ci-ios-diagnostics/watchdog-screenshot.txt 2>&1 || true
            mkdir -p ci-ios-diagnostics/DiagnosticReports
            find ~/Library/Logs/DiagnosticReports -type f -mtime -1 -exec cp {} ci-ios-diagnostics/DiagnosticReports/ \; 2>/dev/null || true
          ) > /dev/null 2>&1 &
          WATCHDOG_PID=$!
          WATCHDOG_IDENTITY=$(ps -o pid=,pgid=,ppid=,lstart= -p "$WATCHDOG_PID")
          set +m

          run_ios_fcm_test() {
            local attempt_log="ci-ios-diagnostics/flutter-test-$1.log"
            local simulator_log_offset
            : > "$attempt_log"
            simulator_log_offset=$(wc -c < ci-ios-diagnostics/simulator.log | tr -d '[:space:]')
            # Evidence and watcher lifetime belong to this invocation only.
            set -m
            (
              set +m
              trap hold_diagnostic_owner EXIT
              bash scripts/recover_ios_vm_service.sh "$DEVICE_ID" \
                "$attempt_log" ci-ios-diagnostics/simulator.log "$simulator_log_offset"
            ) >> ci-ios-diagnostics/vm-service-recovery.log 2>&1 &
            VM_RECOVERY_PID=$!
            VM_RECOVERY_IDENTITY=$(ps -o pid=,pgid=,ppid=,lstart= -p "$VM_RECOVERY_PID")
            set +m
            flutter --verbose test integration_test/fcm_initialization_ios_test.dart --no-pub --dart-define=SENTRY_DSN=https://test@dsn.example.local/0 -d "$DEVICE_ID" \
              2>&1 | tee -a "$attempt_log" ci-ios-diagnostics/flutter-test.log
            TEST_EXIT=${PIPESTATUS[0]}
            # Reap before any FrontBoard retry or post-test simulator action.
            if ! stop_vm_service_recovery; then
              echo '::error::Recovery cleanup failed; refusing further simulator actions.'
              if [ "$TEST_EXIT" -eq 0 ]; then TEST_EXIT=1; fi
              exit "$TEST_EXIT"
            fi
          }

          : > ci-ios-diagnostics/flutter-test.log
          set +e
          run_ios_fcm_test first

          # CoreSimulator can finish installing the bundle before FrontBoard
          # registers it. Retry only that pre-process launch race; application
          # crashes and test failures remain blocking on their first result.
          if [ "$TEST_EXIT" -ne 0 ] && grep -Fq 'is unknown to FrontBoard' ci-ios-diagnostics/flutter-test.log; then
            # A cold dependency build currently takes about 48 minutes on the
            # hosted runner, while a retry reuses Xcode's completed build. Keep
            # the retry available until the existing 80-minute watchdog bound.
            if ios_frontboard_retry_has_headroom "$SECONDS"; then
              echo "::warning::FrontBoard has not registered the installed app; resetting its registration and retrying once."
              {
                echo
                echo "=== reset after FrontBoard registration race ==="
                xcrun simctl terminate "$DEVICE_ID" com.clubhouse.livingpositively 2>/dev/null || true
                xcrun simctl uninstall "$DEVICE_ID" com.clubhouse.livingpositively 2>/dev/null || true
                xcrun simctl bootstatus "$DEVICE_ID" -b
              } 2>&1 | tee -a ci-ios-diagnostics/flutter-test.log
              RESET_EXIT=${PIPESTATUS[0]}

              if [ "$RESET_EXIT" -eq 0 ]; then
                sleep 5
                printf '\n=== retry after FrontBoard registration reset ===\n' | tee -a ci-ios-diagnostics/flutter-test.log
                run_ios_fcm_test retry
              else
                TEST_EXIT=$RESET_EXIT
              fi
            else
              echo "::warning::Skipping the FrontBoard retry after ${SECONDS}s to preserve the final 10 minutes of the 90-minute step boundary."
            fi
          fi
          set -e

          {
            echo "=== flutter test exit ==="
            echo "$TEST_EXIT"
            echo
            echo "=== date ==="
            date -u
            echo
            echo "=== process snapshot ==="
            ps -axo pid,ppid,etime,command | grep -E 'flutter|dart|xcodebuild|simctl|Runner|ios-deploy' | grep -v grep || true
            echo
            echo "=== simulator state ==="
            xcrun simctl list devices "$DEVICE_ID"
          } > ci-ios-diagnostics/post-test.txt 2>&1 || true
          xcrun simctl io "$DEVICE_ID" screenshot ci-ios-diagnostics/post-test-screenshot.png > ci-ios-diagnostics/post-test-screenshot.txt 2>&1 || true
          mkdir -p ci-ios-diagnostics/DiagnosticReports
          find ~/Library/Logs/DiagnosticReports -type f -mtime -1 -exec cp {} ci-ios-diagnostics/DiagnosticReports/ \; 2>/dev/null || true

          exit "$TEST_EXIT"

      - name: Collect iOS launch failure forensics
        if: ${{ always() && steps.ios-integration-test.outcome == 'failure' }}
        timeout-minutes: 5
        run: |
          DIAGNOSTICS_DIR="$PWD/ci-ios-diagnostics"
          mkdir -p "$DIAGNOSTICS_DIR"
          RUNNER_PID=$(sed -n 's/.*com\.clubhouse\.livingpositively: \([0-9][0-9]*\).*/\1/p' "$DIAGNOSTICS_DIR/flutter-test.log" 2>/dev/null | tail -n 1)

          {
            echo "=== post-failure launch assessment ==="
            date -u
            echo
            echo "Runner PID from Flutter log: ${RUNNER_PID:-missing}"
            echo
            echo "=== installed app container ==="
            xcrun simctl get_app_container "$DEVICE_ID" com.clubhouse.livingpositively app || true
          } > "$DIAGNOSTICS_DIR/launch-failure.txt" 2>&1

          if ! grep -q 'Waiting for VM Service port to be available' "$DIAGNOSTICS_DIR/flutter-test.log" 2>/dev/null; then
            echo "Flutter did not reach the VM-service wait; skipping relaunch." >> "$DIAGNOSTICS_DIR/launch-failure.txt"
            exit 0
          fi
          if grep -Eq 'VM Service URL on device|Successfully connected to service protocol' "$DIAGNOSTICS_DIR/flutter-test.log"; then
            echo "Flutter connected to the VM service; skipping dyld relaunch." >> "$DIAGNOSTICS_DIR/launch-failure.txt"
            exit 0
          fi
          if [ -z "$RUNNER_PID" ]; then
            echo "The Runner PID was not present in Flutter's verbose log; skipping relaunch." >> "$DIAGNOSTICS_DIR/launch-failure.txt"
            exit 0
          fi

          # The timed-out Flutter step has ended, so this relaunch cannot be
          # adopted by the test process. It exists only to surface dyld stderr.
          xcrun simctl terminate "$DEVICE_ID" com.clubhouse.livingpositively 2>/dev/null || true
          xcrun simctl launch \
            --console \
            "$DEVICE_ID" \
            com.clubhouse.livingpositively \
            --disable-vm-service-publication \
            > "$DIAGNOSTICS_DIR/manual-launch.log" 2>&1 &
          MANUAL_LAUNCH_PID=$!
          sleep 10
          kill "$MANUAL_LAUNCH_PID" 2>/dev/null || true
          wait "$MANUAL_LAUNCH_PID" 2>/dev/null || true
          xcrun simctl terminate "$DEVICE_ID" com.clubhouse.livingpositively 2>/dev/null || true

          xcrun simctl spawn "$DEVICE_ID" log show \
            --last 3m \
            --style compact \
            --info \
            --debug \
            --predicate 'process == "Runner" OR process == "ReportCrash" OR eventMessage CONTAINS[c] "dyld" OR eventMessage CONTAINS[c] "Library not loaded"' \
            > "$DIAGNOSTICS_DIR/launch-failure-system.log" 2>&1 || true
          sleep 10
          mkdir -p "$DIAGNOSTICS_DIR/DiagnosticReports/host" "$DIAGNOSTICS_DIR/DiagnosticReports/simulator"
          find ~/Library/Logs/DiagnosticReports -type f -mtime -1 -exec cp -n {} "$DIAGNOSTICS_DIR/DiagnosticReports/host/" \; 2>/dev/null || true
          find ~/Library/Developer/CoreSimulator/Devices/"$DEVICE_ID"/data/Library/Logs/CrashReporter -type f -mtime -1 -exec cp -n {} "$DIAGNOSTICS_DIR/DiagnosticReports/simulator/" \; 2>/dev/null || true

      - name: Save Xcode DerivedData
        if: ${{ always() && (steps.ios-integration-test.outcome == 'success' || steps.ios-integration-test.outcome == 'failure') && steps.ios-derived-data-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@v5
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: ${{ steps.ios-derived-data-cache.outputs.cache-primary-key }}

      - name: Upload iOS integration diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-integration-diagnostics
          path: ci-ios-diagnostics
          if-no-files-found: warn

      - name: Upload iOS Podfile lockfile
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-podfile-lock
          path: ios/Podfile.lock
          if-no-files-found: warn

  unit-test-web-telemetry:
    # Phase 10C (ADR-005 § C): TELEMETRY-ONLY web-test job — NOT YET A GATE.
    #
    # This job exists to surface the failure modes of running the unit
    # suite under `flutter test --platform chrome`. Once those failures
    # are triaged and the suite is green under Chrome, this job will be
    # renamed to `unit-test-web`, lose `continue-on-error: true` and the
    # `if: always()` upload, gain a real gate step (e.g. a
    # `scripts/check_web_coverage.dart`), and be wired into the
    # `coverage-aggregate` job's `needs:` list so its lcov merges into
    # the aggregate floor.
    #
    # Until that flip happens, this job is **deliberately green-on-failure**:
    #   1. `continue-on-error: true` on the test step makes a `flutter test`
    #      crash report job-success.
    #   2. The job is NOT in `coverage-aggregate`'s `needs:` list, so a
    #      missing/empty `coverage/lcov.info` cannot block the aggregate.
    #
    # This is intentional. Adding web to the aggregate before the suite is
    # green would either skip-on-failure (unsafe under branch protection)
    # or block all PRs on web flakes. The job's value during this phase is
    # the `coverage-web-lcov` artifact + the test-step log, both of which
    # PR reviewers can inspect to file triage issues.
    #
    # Tracking issue: see § "Still deferred (post Phase 10)" in
    # docs/coverage-status.md.
    runs-on: ubuntu-latest
    env:
      FIREBASE_OPTIONS_PATH: lib/util/Firebase/firebase_options.dart
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Write firebase options file
        env:
          FIREBASE_OPTIONS: ${{ secrets.FIREBASE_OPTIONS }}
        run: |
          printf '%s' "$FIREBASE_OPTIONS" | base64 --decode > "${{ env.FIREBASE_OPTIONS_PATH }}"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.0"
          cache: true

      - name: Install dependencies
        shell: bash
        run: |
          export XDG_CONFIG_HOME="$RUNNER_TEMP/dart-config"
          mkdir -p "$XDG_CONFIG_HOME"
          echo "XDG_CONFIG_HOME=$XDG_CONFIG_HOME" >> "$GITHUB_ENV"
          for attempt in 1 2 3; do
            echo "Installing Dart dependencies (attempt ${attempt}/3)."
            if flutter pub get; then
              exit 0
            fi
            if [[ "$attempt" -lt 3 ]]; then
              delay=$((attempt * 15))
              echo "::warning::flutter pub get failed; retrying in ${delay}s."
              sleep "$delay"
            fi
          done
          exit 1

      - name: Run unit tests on Chrome (telemetry — failures do NOT fail this job)
        # `--platform chrome` compiles the test/ suite via dart2js and runs
        # under headless Chrome. SENTRY_DSN dart-define is the same synthetic
        # value the other jobs use; the AnalyticsService dart-define-merge
        # variant (Phase 6 / ADR-001) is NOT re-run here — that file is
        # already covered by the Android unit job's lcov.
        #
        # `continue-on-error: true` is INTENTIONAL during the triage phase
        # (see job-level comment). Do not remove it without also wiring
        # this job into coverage-aggregate's needs: list and adding a real
        # gate step below.
        continue-on-error: true
        run: |
          flutter test --platform chrome --coverage --dart-define=SENTRY_DSN=https://test@dsn.example.local/0

      - name: Upload web coverage report (telemetry — may be missing or stale)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-web-lcov
          path: coverage/lcov.info
          # Do not fail the upload if the file is missing (it will be when
          # `flutter test --platform chrome` crashes before emitting lcov).
          if-no-files-found: warn

  coverage-aggregate:
    # Phase 8 (ADR-003): aggregate coverage gate — merges unit lcov and
    # integration lcov, enforces 85% global floor on the union.
    #
    # `if: ${{ always() }}` + explicit dependency-result check (first step
    # below) is deliberate: with bare `needs:` alone, GitHub Actions skips
    # this job when either upstream fails, and a SKIPPED required check
    # can be treated as "success" by branch protection rules, allowing a
    # PR with a failed upstream to merge with the aggregate gate looking
    # green. The `always()` pattern forces this job to run on every PR;
    # the first step then fails fast when either dependency did not
    # succeed, so the job's status is ALWAYS one of {success, failure} —
    # never "skipped" — and branch protection treats it correctly.
    #
    # The existing gates are UNCHANGED:
    #   build-android → check_coverage.dart          (85% unit/global floor)
    #   integration-test → check_integration_coverage.dart  (per-file floors)
    # This job is purely additive; removing it restores the Phase 7 state.
    runs-on: ubuntu-latest
    # Functions and iOS do not emit aggregate lcov inputs, but their job
    # results are part of this canonical gate so a required aggregate check
    # cannot pass while either production boundary is red.
    needs: [build-android, integration-test, functions-test, integration-test-ios]
    if: ${{ always() }}
    timeout-minutes: 15
    steps:
      - name: Verify all upstream jobs succeeded
        # Fail fast if any dependency did not succeed. We check the
        # `result` outputs explicitly rather than relying on `needs:` to
        # skip — see job-level comment above for why a skipped required
        # check is unsafe under branch protection.
        run: |
          build_result="${{ needs.build-android.result }}"
          intg_result="${{ needs.integration-test.result }}"
          functions_result="${{ needs.functions-test.result }}"
          ios_result="${{ needs.integration-test-ios.result }}"
          echo "build-android result:        $build_result"
          echo "integration-test result:     $intg_result"
          echo "functions-test result:       $functions_result"
          echo "integration-test-ios result: $ios_result"
          if [ "$build_result" != "success" ]; then
            echo "::error::Upstream job build-android did not succeed (result=$build_result); cannot compute aggregate coverage." >&2
            exit 1
          fi
          if [ "$intg_result" != "success" ]; then
            echo "::error::Upstream job integration-test did not succeed (result=$intg_result); cannot compute aggregate coverage." >&2
            exit 1
          fi
          if [ "$functions_result" != "success" ]; then
            echo "::error::Upstream job functions-test did not succeed (result=$functions_result); cannot compute aggregate coverage." >&2
            exit 1
          fi
          if [ "$ios_result" != "success" ]; then
            echo "::error::Upstream job integration-test-ios did not succeed (result=$ios_result); cannot compute aggregate coverage." >&2
            exit 1
          fi
          echo "All upstream jobs succeeded — proceeding with aggregate merge."

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.0"
          cache: true

      - name: Install dependencies
        shell: bash
        run: |
          export XDG_CONFIG_HOME="$RUNNER_TEMP/dart-config"
          mkdir -p "$XDG_CONFIG_HOME"
          echo "XDG_CONFIG_HOME=$XDG_CONFIG_HOME" >> "$GITHUB_ENV"
          for attempt in 1 2 3; do
            echo "Installing Dart dependencies (attempt ${attempt}/3)."
            if flutter pub get; then
              exit 0
            fi
            if [[ "$attempt" -lt 3 ]]; then
              delay=$((attempt * 15))
              echo "::warning::flutter pub get failed; retrying in ${delay}s."
              sleep "$delay"
            fi
          done
          exit 1

      - name: Download unit coverage artifact
        uses: actions/download-artifact@v4
        with:
          name: coverage-lcov
          path: coverage

      - name: Download integration coverage artifact
        uses: actions/download-artifact@v4
        with:
          name: coverage-integration-lcov
          path: coverage

      - name: Merge unit and Android-integration lcov files
        # Merges coverage/lcov.info (unit) and coverage/integration.info
        # (Android intg) into coverage/aggregate.info by taking the max
        # hit-count per line — same semantics as the ADR-001 Phase-6 merge
        # step. Output path is distinct from all inputs so no upstream
        # gate is clobbered.
        #
        # The iOS simulator job is a behavioral gate and intentionally does
        # not emit a third lcov input.
        run: dart run scripts/merge_lcov.dart coverage/aggregate.info coverage/lcov.info coverage/integration.info

      - name: Enforce aggregate coverage gate
        # check_aggregate_coverage.dart reads coverage/lcov.info and
        # coverage/integration.info directly (parseLcovInputs), applies the
        # same exclude list as check_coverage.dart, and enforces the 89%
        # aggregate global floor (originally 85% under ADR-003 Phase 8;
        # ratcheted to 89% by ADR-004 Phase 9 after the firestore-injection
        # extension in firebase_functions.dart pushed unit-only coverage
        # to 89.33% — see docs/coverage-status.md § Round 9).
        #
        # The iOS simulator job does not contribute lcov; this aggregate gate
        # remains the unit and Android-integration union.
        run: dart run scripts/check_aggregate_coverage.dart

      - name: Upload aggregate coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-aggregate-lcov
          path: coverage/aggregate.info

  # BEGIN NOTIFICATION BACKEND RELEASE
  notification_backend_plan:
    name: Plan notification backend release
    runs-on: ubuntu-latest
    needs: [coverage-aggregate, functions-test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    timeout-minutes: 10
    permissions:
      contents: read
      actions: read
    outputs:
      required: ${{ steps.changes.outputs.required }}
      functions_required: ${{ steps.changes.outputs.functions_required }}
      content_required: ${{ steps.changes.outputs.content_required }}
    steps:
      - name: Checkout tested revision and history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: functions/package-lock.json

      - name: Detect changes since the last successful main run
        id: changes
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api --method GET "repos/$GITHUB_REPOSITORY/actions/workflows/main.yml/runs" \
            -f branch=main -f event=push -f status=success -f per_page=100 \
            > "$RUNNER_TEMP/functions-successful-runs.json"
          node scripts/functions_deployment_changes.mjs "$RUNNER_TEMP/functions-successful-runs.json"

  notification_backend_release:
    name: Release changed notification backend
    runs-on: ubuntu-latest
    needs: [notification_backend_plan]
    if: >-
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      needs.notification_backend_plan.outputs.required == 'true'
    timeout-minutes: 30
    permissions:
      contents: read
    environment: firebase-production
    env:
      FIREBASE_PROJECT_ID: mezilondb
    steps:
      - name: Checkout tested revision and history
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: functions/package-lock.json

      - name: Check deployment credential configuration
        env:
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
          FUNCTIONS_RELEASE_REQUIRED: ${{ needs.notification_backend_plan.outputs.functions_required }}
          NOTIFICATION_MUTATION_FENCE_CLIENT_ROLLOUT_APPROVED: ${{ vars.NOTIFICATION_MUTATION_FENCE_CLIENT_ROLLOUT_APPROVED }}
        run: node scripts/validate_firebase_deployment_config.mjs

      - name: Install Functions dependencies
        run: npm --prefix functions ci

      - name: Reject deployment of a superseded main revision
        run: |
          current_main=$(git ls-remote origin refs/heads/main | cut -f1)
          if [ "$current_main" != "$GITHUB_SHA" ]; then
            echo "::error::main has advanced. Use its new workflow run instead of deploying this older revision."
            exit 1
          fi

      - name: Authenticate Firebase deployment
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3
        with:
          project_id: ${{ env.FIREBASE_PROJECT_ID }}
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}

      - name: Provision changed notification content
        if: needs.notification_backend_plan.outputs.content_required == 'true'
        run: npm --prefix functions run provision:notifications -- --project "$FIREBASE_PROJECT_ID"

      - name: Install Firebase CLI
        if: needs.notification_backend_plan.outputs.functions_required == 'true'
        run: npm install --global firebase-tools@15.29.0

      - name: Deploy Functions to mezilondb
        if: needs.notification_backend_plan.outputs.functions_required == 'true'
        # firebase.json builds TypeScript via predeploy. No --force: unexpected
        # function deletions must fail for review instead of being auto-approved.
        run: firebase deploy --only functions --project "$FIREBASE_PROJECT_ID" --non-interactive

  notification_backend_gate:
    name: Verify notification backend release
    runs-on: ubuntu-latest
    needs: [notification_backend_plan, notification_backend_release]
    if: >-
      always() &&
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main'
    steps:
      - name: Verify required backend work completed
        env:
          PLAN_RESULT: ${{ needs.notification_backend_plan.result }}
          RELEASE_REQUIRED: ${{ needs.notification_backend_plan.outputs.required }}
          RELEASE_RESULT: ${{ needs.notification_backend_release.result }}
        run: |
          if [ "$PLAN_RESULT" != "success" ]; then
            echo "::error::Notification backend change detection did not succeed."
            exit 1
          fi
          if [ "$RELEASE_REQUIRED" = "true" ] && [ "$RELEASE_RESULT" != "success" ]; then
            echo "::error::Required notification backend release did not succeed."
            exit 1
          fi
          if [ "$RELEASE_REQUIRED" != "true" ] && [ "$RELEASE_RESULT" != "skipped" ]; then
            echo "::error::Unexpected notification backend release result."
            exit 1
          fi
  # END NOTIFICATION BACKEND RELEASE

  release-android-internal:
    name: Release Android to Google Play internal testing
    runs-on: ubuntu-latest
    needs:
      [
        build-android,
        coverage-aggregate,
        functions-test,
        integration-test-ios,
        notification_backend_gate,
      ]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    concurrency:
      group: google-play-internal-release
      cancel-in-progress: false
      queue: max
    steps:
      - name: Download App Bundle
        uses: actions/download-artifact@v4
        with:
          name: app-release
          path: build/app/outputs/bundle/release

      - name: Publish to Google Play internal testing
        uses: r0adkll/upload-google-play@e738b9dd8f2476ea806d921b64aacd24f34515a5 # v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: com.matzilon.mezilon
          releaseFiles: build/app/outputs/bundle/release/app-release.aab
          tracks: internal
          status: completed

  build-web:
    runs-on: ubuntu-latest
    needs: [notification_backend_gate]
    if: github.ref == 'refs/heads/main'
    env:
      FIREBASE_OPTIONS_PATH: lib/util/Firebase/firebase_options.dart
      WEB_BUILD_PATH: build/web
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Write firebase options file
        env:
          FIREBASE_OPTIONS: ${{ secrets.FIREBASE_OPTIONS }}
        run: |
          printf '%s' "$FIREBASE_OPTIONS" | base64 --decode > "${{ env.FIREBASE_OPTIONS_PATH }}"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.0"
          cache: true

      - name: Install dependencies
        shell: bash
        run: |
          export XDG_CONFIG_HOME="$RUNNER_TEMP/dart-config"
          mkdir -p "$XDG_CONFIG_HOME"
          echo "XDG_CONFIG_HOME=$XDG_CONFIG_HOME" >> "$GITHUB_ENV"
          for attempt in 1 2 3; do
            echo "Installing Dart dependencies (attempt ${attempt}/3)."
            if flutter pub get; then
              exit 0
            fi
            if [[ "$attempt" -lt 3 ]]; then
              delay=$((attempt * 15))
              echo "::warning::flutter pub get failed; retrying in ${delay}s."
              sleep "$delay"
            fi
          done
          exit 1

      - name: Get version from pubspec.yaml
        id: get_version
        run: |
          VERSION_LINE=$(grep '^version: ' pubspec.yaml)
          VERSION_STRING=${VERSION_LINE#version: }
          echo "VERSION_STRING=$VERSION_STRING" >> $GITHUB_ENV

      - name: Parse version components
        run: |
          VERSION_NO_BUILD="${VERSION_STRING%%+*}"
          echo "VERSION_NO_BUILD=$VERSION_NO_BUILD" >> $GITHUB_ENV

      - name: Get short commit hash
        run: |
          COMMIT_HASH=$(git rev-parse --short HEAD)
          echo "COMMIT_HASH=$COMMIT_HASH" >> $GITHUB_ENV

      - name: Set build name and number
        run: |
          BUILD_NAME="${VERSION_NO_BUILD}+${COMMIT_HASH}"
          BUILD_NUMBER="${GITHUB_RUN_NUMBER}"
          echo "BUILD_NAME=$BUILD_NAME" >> $GITHUB_ENV
          echo "BUILD_NUMBER=$BUILD_NUMBER" >> $GITHUB_ENV

      - name: Build Web App
        env:
          MIXPANEL_PROJECT_TOKEN: ${{ secrets.MIXPANEL_PROJECT_TOKEN }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
        run: |
          flutter build web --release \
            --build-name="${BUILD_NAME}" \
            --build-number="${BUILD_NUMBER}" \
            --dart-define=MIXPANEL_PROJECT_TOKEN="$MIXPANEL_PROJECT_TOKEN" \
            --dart-define=SENTRY_DSN="$SENTRY_DSN"

      - name: Upload Web Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: web-build
          path: ${{ env.WEB_BUILD_PATH }}

      - name: Deploy to Azure Storage Static Website
        uses: azure/cli@v2
        with:
          inlineScript: |
            az storage blob upload-batch \
              --account-name ${{ secrets.AZURE_WEB_STORAGE_ACCOUNT }} \
              --account-key ${{ secrets.AZURE_WEB_STORAGE_KEY }} \
              --destination '$web' \
              --source build/web \
              --overwrite
  build-dev-web:
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main'
    concurrency:
      group: dev-web-${{ github.ref }}
      cancel-in-progress: false
    env:
      FIREBASE_OPTIONS_PATH: lib/util/Firebase/firebase_options.dart
      WEB_BUILD_PATH: build/web
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Write firebase options file
        env:
          FIREBASE_OPTIONS: ${{ secrets.FIREBASE_OPTIONS }}
        run: |
          printf '%s' "$FIREBASE_OPTIONS" | base64 --decode > "${{ env.FIREBASE_OPTIONS_PATH }}"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.0"
          cache: true

      - name: Install dependencies
        shell: bash
        run: |
          export XDG_CONFIG_HOME="$RUNNER_TEMP/dart-config"
          mkdir -p "$XDG_CONFIG_HOME"
          echo "XDG_CONFIG_HOME=$XDG_CONFIG_HOME" >> "$GITHUB_ENV"
          for attempt in 1 2 3; do
            echo "Installing Dart dependencies (attempt ${attempt}/3)."
            if flutter pub get; then
              exit 0
            fi
            if [[ "$attempt" -lt 3 ]]; then
              delay=$((attempt * 15))
              echo "::warning::flutter pub get failed; retrying in ${delay}s."
              sleep "$delay"
            fi
          done
          exit 1

      - name: Get version from pubspec.yaml
        id: get_version
        run: |
          VERSION_LINE=$(grep '^version: ' pubspec.yaml)
          VERSION_STRING=${VERSION_LINE#version: }
          echo "VERSION_STRING=$VERSION_STRING" >> $GITHUB_ENV

      - name: Parse version components
        run: |
          VERSION_NO_BUILD="${VERSION_STRING%%+*}"
          echo "VERSION_NO_BUILD=$VERSION_NO_BUILD" >> $GITHUB_ENV

      - name: Get short commit hash
        run: |
          COMMIT_HASH=$(git rev-parse --short HEAD)
          echo "COMMIT_HASH=$COMMIT_HASH" >> $GITHUB_ENV

      - name: Set build name and number
        run: |
          BUILD_NAME="${VERSION_NO_BUILD}+${COMMIT_HASH}"
          BUILD_NUMBER="${GITHUB_RUN_NUMBER}"
          echo "BUILD_NAME=$BUILD_NAME" >> $GITHUB_ENV
          echo "BUILD_NUMBER=$BUILD_NUMBER" >> $GITHUB_ENV

      - name: Build Web App
        env:
          MIXPANEL_PROJECT_TOKEN: ${{ secrets.MIXPANEL_PROJECT_TOKEN }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
        run: |
          flutter build web --release \
            --build-name="${BUILD_NAME}" \
            --build-number="${BUILD_NUMBER}" \
            --dart-define=MIXPANEL_PROJECT_TOKEN="$MIXPANEL_PROJECT_TOKEN" \
            --dart-define=SENTRY_DSN="$SENTRY_DSN"

      - name: Upload Web Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: web-build-dev
          path: ${{ env.WEB_BUILD_PATH }}

      - name: Deploy to Azure Storage Static Website
        uses: azure/cli@v2
        with:
          inlineScript: |
            az storage blob upload-batch \
              --account-name ${{ secrets.AZURE_DEV_WEB_STORAGE_ACCOUNT }} \
              --account-key ${{ secrets.AZURE_DEV_WEB_STORAGE_KEY }} \
              --destination '$web' \
              --source build/web \
              --overwrite

     
